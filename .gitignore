node_modules/
dist/
data/
.env
*.db
*.db-journal
.DS_Store
test.txt
paw_logo_blue_border_*.jpg
# Local Claude Code MCP config (points at a specific project ref — env-specific)
.mcp.json
# Security-scan scratch output (probe artifacts, never source)
.probe/
# Bun project — the npm lockfile shouldn't be committed (use bun.lockb)
package-lock.json

# Generated docs (served dynamically; static copies are optional)
llms.txt
llms-full.txt

# Design handoff scratch (HTML/jsx prototypes + screenshots from the Anthropic
# design tool) — reference only; the shipped design notes live in docs/design/.
Interactive Data Visualization/
Skill Layout Design/

# Internal working notes: task prompts + private business docs. The prompts dir
# was once tracked (files predate the ignore rule) — untracked deliberately.
docs/prompts/
docs/internal/private/

# Secret-scan output (Phase D) — the report must never be committed
gitleaks-report.json

# Agent-tooling state (added by `skills add`): local skill cache + symlinks +
# lockfile. Machine-local — .claude/skills holds symlinks that break on other
# checkouts. Not paw source.
.agents/
.claude/
skills-lock.json
