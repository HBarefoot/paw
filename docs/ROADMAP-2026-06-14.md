# Paw — open loops & clear-it roadmap (2026-06-14)

Consolidated from the session. Ordered so the trailing 10% lands first, then the items that actually
move the strategy, then parked work with the gate that unparks each.

## A. Close-out — finish the last 10% (do first; mostly merges + deploy)

1. **Merge the open PRs:**
   - `#145` — pairing: stable codes + `security.ownerUserIds`.
   - `feat/access-approvals-page` — the `/access` one-click admin approval page.
   - `#146` — companion smooth avatar zoom.
2. **Finish pairing (after #145 + /access merge):** set `security.ownerUserIds` to your Slack user
   id, redeploy → you're auto-recognized, no more pairing prompts; admins approve others at `/access`.
3. **Reconcile prod vs `main` (IMPORTANT).** The companion light-bg fix is in code (`20a12c5`, `:root`
   = `#ffffff`) but prod showed a **black stage** in the browser. Confirm prod is actually serving
   current `main` (Railway auto-deploys, but companion static CSS is cache-busted per boot — a stale
   deploy or cached asset would explain it). Redeploy / hard-refresh and re-check. Several "broken in
   prod" symptoms may just be a behind-main deploy.
4. **Tag the releases:** `v0.6.0` (#137) and `v0.7.0` (#140) merged + bumped `package.json` but were
   never tagged. Tag both from `main` and push. Going forward, tag on every release.
5. **Visual eyeball pass (I can do in-browser):** once prod is current — companion light + dark
   legibility, smooth zoom (#146), emerald accent across tabs. Clears the accumulated unverified-visual
   debt in one pass.

## B. Validate — needs your hands (the real value gates)

6. **Run the Vercel PoC against ONE real page.** Connect `vercel.token` in `/vault`, do the one-time
   Vercel↔GitHub dashboard link, have the agent publish a real static page, read the failure-mode map.
   This is the "is the deploy chain reliable?" gate — nothing in Vercel Phase 2 should start until this
   proves out against a real page.
7. **Adopt the optimized orchestrator system prompt.** Drop it into `customPrompt`, and decide its two
   judgment calls: (a) Slack-only vs global framing (if it runs on web/canvas too, the Slack framing
   misfires), (b) the "never build, always delegate" rule vs a canvas-build exception.

## C. Small tech debt (when convenient)

8. **Generalize the approval queue naming.** It's GitHub-named but now serves Vercel + canvas, so
   gated actions get audit-labeled `github.*` (e.g. a `vercel_create_project` approval). Rename /
   generalize so the audit trail is accurate.

## D. Decisions — RESOLVED (2026-06-14)

9. **Kanban → ConstructAI jobs/tasks operations board.** DECIDED. Fork work on real data (PRD §9.2).
   Spec + build when a real builder will use it; not before.
10. **Shared-page auth → gate in paw (kernel), reuse the existing session.** DECIDED for the
    internal case: shared canvas pages require a logged-in paw admin — same auth the console uses,
    near-zero build, "gated alongside paw." Lives in the kernel so forks inherit it.
    - "Not public / not crawled" alone needs NO auth (share token + `noindex` already covers it).
    - External-client **view-only magic-link** grants (client sees one page, not the console/vault)
      are PARKED until a real client needs to be shared a page.
11. **Cross-channel context → leave as-is.** DECIDED. Memory shares *facts* across Slack/web; chat
    *history* stays siloed per channel. No build.

## E. Parked by design — don't build until pulled (gate that unparks it)

- **Vercel Phase 2** (edit→commit→redeploy loop, public-site↔Supabase, deploy observability) — unparks
  when the PoC (#6) proves reliable **and** a real page needs maintaining.
- **Sense → decide → act metrics loop** (the strategic differentiator: analytics in, recommend/act
  out) — unparks once the plumbing is reliable and there's a real business to sense.
- **Multi-tenancy** (per-user GitHub/Vercel OAuth, token isolation) — unparks only when a real user
  validates the single-operator version. Biggest new risk; keep it parked longest.
- **Generic "any business" ops board, custom domains (Cluster B), prompt-actions chat+library
  (Cluster A)** — unpark when a concrete need pulls each.

## The honest headline
The trailing 10% in **A** should land before anything new. The only two items that move the
*strategy* are **#6 (validate Vercel against a real page)** and **putting ConstructAI in front of a
real builder** — everything in E is downstream of one of those. The constraint is still a
customer/validation, not more features.
